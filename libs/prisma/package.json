{
  "name": "@prisma/client",
  "version": "1.0.0",
  "description": "",
  "main": "./index.ts",
  "types": "./index.ts",
  "scripts": {
    "build": "prisma generate && tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "pre": "prisma generate && npm ci"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^4.5.0",
    "typescript": "~4.6"
  },
  "devDependencies": {
    "prisma": "^4.5.0"
  }
}

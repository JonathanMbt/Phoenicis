{
  "pipeline": {
    "build": {
      "outputs": ["dist/**", "build/**"],
      "dependsOn": ["^build", "pre"],
      "inputs": ["**/**/*.tsx", "**/**/*.ts"]
    },
    "lint": {
      "outputs": []
    },
    "dev": {
      "cache": false
    },
    "pre": {
      "cache": false
    }
  }
}
